spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/centrocultural?user=postgres&password=postgres&ssl=false
spring.datasource.username=postgres
spring.datasource.password=postgres

# Create the database entities for the first time
# To update the tables change create to update
spring.jpa.hibernate.ddl-auto = update

spring.data.rest.base-path=/api